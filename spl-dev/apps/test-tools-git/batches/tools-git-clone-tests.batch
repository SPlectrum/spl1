tools/git/clone -h --repo test-repo
tools/git/clone --help --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --repo test-repo
tools/git/clone --url git@github.com:user/repo.git --repo test-repo
tools/git/clone --url https://gitlab.com/user/repo.git --repo test-repo
tools/git/clone --url /path/to/local/repo.git --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --directory my-repo --repo test-repo
tools/git/clone --url https://github.com/user/repo.git -d my-repo --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --directory "my project" --repo test-repo
tools/git/clone --url https://github.com/user/repo.git -d "my project" --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --branch main --repo test-repo
tools/git/clone --url https://github.com/user/repo.git -b main --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --branch develop --repo test-repo
tools/git/clone --url https://github.com/user/repo.git -b feature-branch --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --depth 1 --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --depth 5 --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --depth 10 --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --directory my-repo --branch main --repo test-repo
tools/git/clone --url https://github.com/user/repo.git -d my-repo -b main --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --directory my-repo --depth 1 --repo test-repo
tools/git/clone --url https://github.com/user/repo.git -d my-repo --depth 1 --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --branch main --depth 1 --repo test-repo
tools/git/clone --url https://github.com/user/repo.git -b main --depth 1 --repo test-repo
tools/git/clone --url https://github.com/user/repo.git --directory my-repo --branch main --depth 1 --repo test-repo
tools/git/clone --url https://github.com/user/repo.git -d my-repo -b main --depth 1 --repo test-repo
tools/git/clone --url https://github.com/user/repo.git -r /target/path
tools/git/clone --url https://github.com/user/repo.git --repo /target/path
tools/git/clone -h --url https://github.com/user/repo.git --repo test-repo
tools/git/clone --help --branch main --repo test-repo